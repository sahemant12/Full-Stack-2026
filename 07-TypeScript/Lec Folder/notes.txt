// How to initialize Ts file:
// 1. npm init -y
// 2. tsc --init : write a Ts configuration file.
// 3. search in npm package on internet: @types/node
// https://www.npmjs.com/package/@types/node
// npm i @types/node -D
// npm i typescript -D

// 4. npm i winston // for logger
// 5. npm i @types/express -D // when importing express

// 6. Our application is done. Now, to run Ts 1st we have to compile it.
// uncomment in tsconfig.json
//     "rootDir": "./src",
//     "outDir": "./dist",

// 7. script file:
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc -p ."
  },
